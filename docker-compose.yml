version: "3.7"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: devserver
    container_name: crypto-webpack
    volumes:
      - ./src:/app/src
    ports:
      - 8080:80